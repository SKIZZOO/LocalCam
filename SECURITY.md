# Security

`config.json` is local-only and ignored by Git. Do not commit camera passwords.

Keep LocalCam on a private LAN. Do not port-forward port 8765 to the public internet. The current web UI does not implement HTTPS or strong authentication.
